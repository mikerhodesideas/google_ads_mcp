# Google Ads MCP Server Environment Variables
# Copy this file to .env and fill in your values

# Path to your google-ads.yaml credentials file
# Default: ./google-ads.yaml (in the same directory as this file)
GOOGLE_ADS_CREDENTIALS=./google-ads.yaml

# Optional: Use Google OAuth access token instead of refresh token
# Set to "true" to enable
# USE_GOOGLE_OAUTH_ACCESS_TOKEN=

# Optional: Google OAuth credentials for FastMCP server auth
# Only needed if you want to use OAuth authentication with the server
# FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_ID=
# FASTMCP_SERVER_AUTH_GOOGLE_CLIENT_SECRET=
# FASTMCP_SERVER_BASE_URL=http://localhost:8000
